params {
    samplesheet          = "${projectDir}/samples.tsv"
    outdir               = "${projectDir}/demo_results"

    model_file           = "assets/models/XuanjiNovo_130M_massnet_massivekb.ckpt"
    cascadia_model_file  = "assets/models/cascadia.ckpt"

    mode                 = "denovo"
}

docker.enabled = true

process {
    executor = 'local'

    withName: RUN_XUANJINOVO {
        container = 'densve/xuanjinovo-novotax:cuda11_v1.1.1'
        containerOptions = '--gpus all'
        cpus = 4
        memory = '12 GB'
    }

    withName: RUN_CASCADIA {
        container = 'densve/cascadia-novotax:cuda11_v1.1'
        containerOptions = '--gpus all'
        cpus = 4
        memory = '12 GB'
    }

    withName: RUN_NOVOTAX {
        container = 'densve/novotax:latest'
        cpus = 1
        memory = '1 GB'
    }
}